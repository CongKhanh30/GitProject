class HocVien{
    name;
    age;
    img;

    // Tạo constructor(tạo ra đối tượng) trong class: generate => constructor => ctrl A => ok
    constructor(name, age, img) {
        this.name = name;
        this.age = age;
        this.img = img;
    }
}
